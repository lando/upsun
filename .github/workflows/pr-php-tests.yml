name: PHP Tests

# Quarantined until CI Docker / a broader matrix exists. Local OPEN spike already done once.
# Restore the Leia matrix from git history when CI can run docker pull + lando start across examples.
on:
  pull_request:
  workflow_dispatch:

jobs:
  leia-test:
    runs-on: ubuntu-22.04
    steps:
      - name: Quarantine notice
        run: |
          echo "Leia/OPEN jobs stay quarantined until CI Docker / a broader matrix exists (local OPEN spike already done once)."
          echo "See docs/parity/06-image-spike.md"
