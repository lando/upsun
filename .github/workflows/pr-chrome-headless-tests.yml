name: Headless Chrome Tests

# Quarantined until a live Docker OPEN spike exists.
# Restore the Leia matrix from git history after docker pull + lando start is proven.
on:
  pull_request:
  workflow_dispatch:

jobs:
  leia-test:
    runs-on: ubuntu-22.04
    steps:
      - name: Quarantine notice
        run: |
          echo "Leia/OPEN jobs are quarantined until a live Docker OPEN spike exists."
          echo "See docs/parity/06-image-spike.md"
